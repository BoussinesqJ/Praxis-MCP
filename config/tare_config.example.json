{
  "mcpServers": {
    "praxis": {
      "command": "praxis",
      "args": ["serve"],
      "env": {
        "PRAXIS_WORKSPACE": "${PRAXIS_WORKSPACE}",
        "PRAXIS_TOOLS_TIER": "admin"
      }
    }
  }
}

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ── Virtual Environment ──
.venv/
venv/
ENV/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── pytest ──
.pytest_cache/
.coverage
htmlcov/

# ── Obsidian ──
.obsidian/
obsidian/.obsidian/

# ── 临时文件 ──
*.tmp
*.log

# ── 工具内部数据（本地 workspace）──
.workbuddy/
.reasonix/
.codegraph/
reasonix.toml

# ── 行情/状态缓存（可重建）──
data/market_cache/
data/state/
data/reports/

# ── Prompt 版本测试数据 ──
data/prompt_versions/

# ── 日志 ──
data/logs/

# ── 真实交易数据（含敏感持仓，不进仓库）──
data/ledger/transactions.jsonl
data/ledger/pending.jsonl
data/decisions/decision_records.jsonl
data/nav/
data/audit/

# ── 真实投资者数据 ──
investors/user/

# ── 策略备份（自动生成）──
strategies/*.bak

# ── 测试输出模板 ──
teams/output_templates/test_tpl.md

# ── 审批/测试数据 ──
审批/
测试审批/

# ── 用户插件（本地开发，不进仓库）──
providers/*.py
!providers/__init__.py
!providers/_example_provider.py

# ── 敏感文件 ──
*.key
*.pem
*.secret

# ── 含真实路径的配置 ──
config/claude_desktop_config.json
config/trae_config.json
config/trae_cn_mcp.json
config/opencode_config.json
config/tare_config.json
config/workbuddy_config.json
config/niuma_ai_config.json

# ── 设计文档（本地保留）──
PRAXIS 详细实施计划-V2.0.md
PRAXIS 投研纪律系统设计文档-V2.0.md
PRAXIS 详细实施计划-V2.1-Phase6.md
【GPT】PRAXIS方案评价与整改建议.md

# ── Windows ──
nul

# ── SQLite ──
*.db
data/*.db

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ── Virtual Environment ──
.venv/
venv/
ENV/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── pytest ──
.pytest_cache/
.coverage
htmlcov/

# ── Obsidian ──
.obsidian/

# ── 临时文件 ──
*.tmp
*.log

# ── 工具内部数据（本地 workspace）──
.workbuddy/
.reasonix/
.codegraph/
reasonix.toml

# ── 运行时数据（可重建，不进仓库）──
data/market_cache/
data/state/
data/reports/
data/logs/
data/evolution_memory/
data/sentinel_history.jsonl
data/prompt_versions/

# ── 真实交易数据（含敏感持仓，不进仓库）──
data/ledger/transactions.jsonl
data/ledger/pending.jsonl
data/decisions/decision_records.jsonl
data/nav/default.jsonl
data/audit/prompt_changes.jsonl

# ── 真实投资者数据 ──
investors/user/
investors/sanchisheng/

# ── 策略备份（自动生成）──
strategies/*.bak

# ── 用户插件（本地开发，不进仓库）──
providers/*.py
!providers/__init__.py
!providers/_example_provider.py
!providers/alphaear_stock_provider.py

# ── 敏感文件 ──
*.key
*.pem
*.secret

# ── 含真实路径的配置 ──
config/claude_desktop_config.json
config/trae_config.json
config/trae_cn_mcp.json
config/opencode_config.json
config/tare_config.json
config/workbuddy_config.json
config/niuma_ai_config.json

# ── Windows ──
nul

# ── 审批/测试数据 ──
审批/
测试审批/
